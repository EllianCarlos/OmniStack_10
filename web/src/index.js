import React from 'react';
import ReactDOM from 'react-dom';

import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));

// Tudo acima é padrão de qualquer aplicação React

// JSX (JavaScript + HTML)
// Se tem JSX o React precisa ser importado no arquivo